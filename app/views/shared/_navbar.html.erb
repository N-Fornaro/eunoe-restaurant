<header aria-label="Site Header" class="sticky top-0 left-0 right-0 z-20" style="<%= "background-color: rgba(0,0,0,0.6)" if current_page?(admin_path) || current_page?(privatisation_path) || current_page?(new_user_session_path) %>; margin-top: -64px;">
  <%# Prob current_page?(root_path) ? 'fixed bg-transparent' : 'sticky bg-black' -> no nav bar after the banner and no possibility with the opacity. AND, height with "mb-24" at the end of the tag header -> add an space btw Nav & banner to others pages(restaurant, cuisine) %>
  <div class="max-w-screen-xl px-4 mx-auto sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="md:flex md:items-center md:gap-12">
        <a class="block" href="/">
          <%= image_tag "Eunoe_LogoB_crop.png", class: "", width: "120px" %>
        </a>
      </div>

      <div class="hidden md:block">
        <nav aria-label="Site Nav" role="menu">
          <ul class="flex items-center gap-10 text-base">
            <li><%= link_to "Accueil", root_path, class: "text-white transition hover:text-white/75" %></li>

            <li><%= link_to "Restaurant", restaurant_path, class: "text-white transition hover:text-white/75" %></li>

            <li><%= link_to "Menu", meals_path, class: "text-white transition hover:text-white/75" %></li>

            <li><%= link_to "Privatisation", privatisation_path, class: "text-white transition hover:text-white/75" %></li>

            <li><%= link_to "Réservation", new_booking_path, class: "text-white transition hover:text-white/75" %></li>

            <% if current_user %>
              <li><%= link_to "Admin", admin_path, class: "text-pink-pastel transition hover:text-white/75" %></li>
            <% end %>

          </ul>
        </nav>
      </div>

      <div class="flex items-center gap-4">
        <div class="sm:flex sm:gap-4">
          <%= link_to "Réserver", new_booking_path, class: "rounded-md bg-pink-pastel px-8 py-2.5 uppercase text-base font-medium text-white shadow" %>
        </div>

        <div class="block md:hidden">
          <button
            class="p-2 text-gray-600 transition bg-gray-100 rounded dropdown-toggle hover:text-gray-600/75"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-5 h-5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M4 6h16M4 12h16M4 18h16"
              />
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>

</header>
